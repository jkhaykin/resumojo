<%= form_for @resupload, :html => { :multipart => true } do |f| %>
  <% if @resupload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resupload.errors.count, "error") %> prohibited this resupload from being saved:</h2>

      <ul>
      <% @resupload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.file_field :document, :class => "btn" %>

  <div class="actions">
    <%= f.submit "Upload", :class => "btn btn-success" %>
  </div>
<% end %>
